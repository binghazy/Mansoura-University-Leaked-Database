# Flask session secret. Use a long random value in production.
SECRET_KEY=

# Public app URL used in admin notification emails.
APP_BASE_URL=http://127.0.0.1:5000

# Optional Vercel/admin login. Set these in Vercel Environment Variables.
ADMIN_EMAIL=
ADMIN_PASSWORD=
ADMIN_NAME=Admin

# SMTP settings for request-access notification emails.
SMTP_HOST=
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_FROM=
SMTP_USE_SSL=false
ACCESS_NOTIFICATION_RECIPIENTS=

# Optional data/user file overrides.
MEMBERS_DATA_FILE=cleaned_members.json
ACCESS_USERS_FILE=
